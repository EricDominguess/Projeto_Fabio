{
  "name": "workout-tracker",
  "version": "1.0.0",
  "description": "Projeto AngularJS com Tailwind",
  "scripts": {
    "start": "live-server --open=index.html",
    "build:css": "tailwindcss -i ./src/input.css -o ./css/styles.css --watch"
  },
  "devDependencies": {
    "live-server": "^1.2.2",
    "tailwindcss": "^3.4.1"
  }
}
